<%# creer une boucle pour afficher toutes les tasks %>
<div>
  <ul>
    <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %>

  <%= link_to "Delete",
  task_path(task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%>

    <%# task_path(path) indique qu'on va dans routes et as:task 'tasks/:id', to: 'tasks#show' %>
    <%# coder la methode show %>
    <% end%>
  </ul>
</div>

<%=link_to 'create a new task', tasks_new_path %>
